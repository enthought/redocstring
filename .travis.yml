language: python

python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4

before_install:
  - pip install haas
  - pip install unittest2
  - pip install coverage
  - pip install coveralls

script:
  - coverage run -m haas -v
  - coverage report -m

after_success:
  - coveralls
